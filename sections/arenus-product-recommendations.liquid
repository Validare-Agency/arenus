{%- comment -%}
  Arenus — You May Also Like (Product Recommendations)
  Identical to arenus-products carousel but uses Shopify's product recommendations API
  instead of a collection picker.
{%- endcomment -%}

<div
  id="ArenusProductRecs-{{ section.id }}"
  class="arenus-product-recs c-page-width"
  style="
    --arenus-product-recs-bg: {{ section.settings.background_color }};
    --arenus-product-recs-text: {{ section.settings.text_color }};
    --arenus-product-recs-image-bg: {{ settings.product_card_image_bg }};
    padding-block-start: {{ section.settings.padding_top }}px;
    padding-block-end: {{ section.settings.padding_bottom }}px;
  "
  data-url="{{ routes.product_recommendations_url }}?section_id={{ section.id }}&product_id={{ product.id }}&limit={{ section.settings.max_products }}"
>
  <div class="arenus-product-recs__header">
    {%- if section.settings.heading != blank -%}
      <h2 class="arenus-product-recs__heading">{{ section.settings.heading }}</h2>
    {%- endif -%}

    <div class="arenus-product-recs__nav" style="display:none;">
      <button
        type="button"
        class="arenus-product-recs__nav-btn arenus-product-recs__nav-btn--prev"
        aria-label="Previous products"
        data-direction="prev"
      >
        {% render 'icon-caret-left' %}
      </button>
      <button
        type="button"
        class="arenus-product-recs__nav-btn arenus-product-recs__nav-btn--next"
        aria-label="Next products"
        data-direction="next"
      >
        {% render 'icon-caret-right' %}
      </button>
    </div>
  </div>

  {%- if recommendations.performed and recommendations.products.size > 0 -%}
    <div class="arenus-product-recs__carousel">
      <div class="arenus-product-recs__track">
        {%- for product in recommendations.products limit: section.settings.max_products -%}
          <div class="arenus-product-recs__card">
            {%- comment -%} Product image {%- endcomment -%}
            <a href="{{ product.url }}" class="arenus-product-recs__card-link" aria-label="{{ product.title | escape }}" tabindex="-1"></a>
            <div class="arenus-product-recs__card-image">
              {%- if product.featured_image != blank -%}
                {{ product.featured_image | image_url: width: 800 | image_tag: loading: 'lazy' }}
              {%- endif -%}

              {%- comment -%} Quick add-to-cart button (inside image for desktop overlay) {%- endcomment -%}
              <button
                type="button"
                class="arenus-product-recs__add-btn{% unless product.available %} is-sold-out{% endunless %}"
                data-product-id="{{ product.id }}"
                data-variant-id="{{ product.selected_or_first_available_variant.id }}"
                {% if product.has_only_default_variant %}
                  data-single-variant="true"
                {% endif %}
                {% unless product.available %}disabled{% endunless %}
                aria-label="{% if product.available %}Add {{ product.title | escape }} to cart{% else %}{{ product.title | escape }} is sold out{% endif %}"
              >
                {%- if product.available -%}
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                    <path d="M22.4893 18.5737L21.1525 7.32375C21.1091 6.95721 20.9321 6.61952 20.6554 6.37529C20.3786 6.13106 20.0215 5.99744 19.6525 6H16.4996C16.4996 4.80653 16.0255 3.66193 15.1816 2.81802C14.3377 1.97411 13.1931 1.5 11.9996 1.5C10.8062 1.5 9.66157 1.97411 8.81766 2.81802C7.97374 3.66193 7.49964 4.80653 7.49964 6H4.34308C3.97399 5.99744 3.61691 6.13106 3.34016 6.37529C3.06342 6.61952 2.88644 6.95721 2.84308 7.32375L1.5062 18.5737C1.4817 18.7837 1.50186 18.9965 1.56536 19.1981C1.62885 19.3998 1.73425 19.5857 1.87464 19.7438C2.01604 19.9025 2.18932 20.0296 2.38317 20.1168C2.57701 20.204 2.78707 20.2494 2.99964 20.25H20.9921C21.206 20.2505 21.4175 20.2056 21.6127 20.1183C21.8079 20.0311 21.9824 19.9034 22.1246 19.7438C22.2644 19.5854 22.3691 19.3993 22.4319 19.1977C22.4948 18.9961 22.5143 18.7835 22.4893 18.5737ZM11.9996 3C12.7953 3 13.5584 3.31607 14.121 3.87868C14.6836 4.44129 14.9996 5.20435 14.9996 6H8.99964C8.99964 5.20435 9.31571 4.44129 9.87832 3.87868C10.4409 3.31607 11.204 3 11.9996 3ZM2.99964 18.75L4.34308 7.5H7.49964V9.75C7.49964 9.94891 7.57866 10.1397 7.71931 10.2803C7.85996 10.421 8.05073 10.5 8.24964 10.5C8.44855 10.5 8.63932 10.421 8.77997 10.2803C8.92062 10.1397 8.99964 9.94891 8.99964 9.75V7.5H14.9996V9.75C14.9996 9.94891 15.0787 10.1397 15.2193 10.2803C15.36 10.421 15.5507 10.5 15.7496 10.5C15.9486 10.5 16.1393 10.421 16.28 10.2803C16.4206 10.1397 16.4996 9.94891 16.4996 9.75V7.5H19.6637L20.9921 18.75H2.99964Z" fill="currentColor"/>
                  </svg>
                  ADD TO CART
                {%- else -%}
                  SOLD OUT
                {%- endif -%}
              </button>
            </div>

            {%- comment -%} Product info {%- endcomment -%}
            <div class="arenus-product-recs__card-info">
              {%- comment -%} Stamped star rating badge {%- endcomment -%}
              <div class="arenus-product-recs__rating">
                <span class="stamped-product-reviews-badge" data-id="{{ product.id }}" data-product-sku="{{ product.handle }}" data-product-type="{{ product.type }}" data-product-title="{{ product.title | escape }}" style="display:block;"></span>
              </div>

              {%- comment -%} Title + price row {%- endcomment -%}
              <div class="arenus-product-recs__title-price">
                <span class="arenus-product-recs__title">{{ product.title }}</span>
                <span class="arenus-product-recs__price">
                  {%- render 'format-price', price: product.selected_or_first_available_variant.price -%}
                </span>
              </div>

              {%- comment -%} Tag badges from metafield {%- endcomment -%}
              {%- assign badges = product.metafields.custom.product_card_badges.value -%}
              {%- if badges != blank and badges.size > 0 -%}
                <div class="arenus-product-recs__tags">
                  {%- for badge in badges -%}
                    <span class="arenus-product-recs__tag">{{ badge }}</span>
                  {%- endfor -%}
                </div>
              {%- endif -%}
            </div>

            {%- comment -%} Variant data for quick-add modal (JSON) {%- endcomment -%}
            {%- unless product.has_only_default_variant -%}
              {%- assign sp_group = product.selling_plan_groups | first -%}
              <script type="application/json" class="arenus-product-recs__card-data">
                {
                  "id": {{ product.id }},
                  "handle": {{ product.handle | json }},
                  "title": {{ product.title | json }},
                  "url": {{ product.url | json }},
                  "image": {{ product.featured_image | image_url: width: 400 | json }},
                  "options": {{ product.options_with_values | json }},
                  {%- if sp_group and sp_group.selling_plans.size > 0 -%}
                  "selling_plan_group": {
                    "id": {{ sp_group.id | json }},
                    "plans": [
                      {%- for plan in sp_group.selling_plans -%}
                        { "id": {{ plan.id }}, "name": {{ plan.name | json }} }{% unless forloop.last %},{% endunless %}
                      {%- endfor -%}
                    ]
                  },
                  {%- endif -%}
                  "variants": [
                    {%- for variant in product.variants -%}
                      {
                        "id": {{ variant.id }},
                        "available": {{ variant.available }},
                        "price": {{ variant.price }},
                        "compare_at_price": {{ variant.compare_at_price | default: 0 }},
                        "options": {{ variant.options | json }},
                        "selling_plan_allocations": [
                          {%- for alloc in variant.selling_plan_allocations -%}
                            { "selling_plan_id": {{ alloc.selling_plan_id }}, "price": {{ alloc.price }} }{% unless forloop.last %},{% endunless %}
                          {%- endfor -%}
                        ]
                      }{%- unless forloop.last -%},{%- endunless -%}
                    {%- endfor -%}
                  ]
                }
              </script>
            {%- endunless -%}
          </div>
        {%- endfor -%}
      </div>
    </div>
  {%- endif -%}

  {%- comment -%} Quick-add modal (shared, single instance per section) {%- endcomment -%}
  <dialog class="arenus-qam__dialog" id="arenus-qam-recs-{{ section.id }}">
    <div class="arenus-qam">
      <button type="button" class="arenus-qam__close" aria-label="Close">
        <svg width="24" height="24" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path d="M12 2L2 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          <path d="M12 12L2 2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </button>

      {%- comment -%} Product header: image + stars + title {%- endcomment -%}
      <div class="arenus-qam__header">
        <div class="arenus-qam__image">
          <img src="" alt="" width="80" height="80" loading="lazy" />
        </div>
        <div class="arenus-qam__meta">
          <div class="arenus-qam__rating">
            <span class="stamped-product-reviews-badge arenus-qam__stamped-badge" style="display:block;"></span>
          </div>
          <h3 class="arenus-qam__title"></h3>
          <div class="arenus-qam__price-wrap">
            <span class="arenus-qam__price"></span>
            <span class="arenus-qam__compare-price"></span>
          </div>
        </div>
      </div>

      {%- comment -%} Variant option groups (built dynamically by JS) {%- endcomment -%}
      <div class="arenus-qam__options"></div>

      {%- comment -%} Subscription selector (rendered dynamically by JS) {%- endcomment -%}
      <div class="arenus-qam__subscription" data-sub-wrap></div>

      {%- comment -%} Footer: quantity stepper + add to cart {%- endcomment -%}
      <div class="arenus-qam__footer">
        <div class="arenus-qam__stepper">
          <button type="button" class="arenus-qam__stepper-btn arenus-qam__stepper-minus" aria-label="Decrease quantity">
            <svg width="16" height="16" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <path d="M2.75 7H11.25" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
            </svg>
          </button>
          <span class="arenus-qam__stepper-value">1</span>
          <button type="button" class="arenus-qam__stepper-btn arenus-qam__stepper-plus" aria-label="Increase quantity">
            <svg width="16" height="16" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <path d="M2.75 7H11.25" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
              <path d="M7 2.75L7 11.25" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
            </svg>
          </button>
        </div>
        <button type="button" class="arenus-qam__add-btn" disabled>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M22.4893 18.5737L21.1525 7.32375C21.1091 6.95721 20.9321 6.61952 20.6554 6.37529C20.3786 6.13106 20.0215 5.99744 19.6525 6H16.4996C16.4996 4.80653 16.0255 3.66193 15.1816 2.81802C14.3377 1.97411 13.1931 1.5 11.9996 1.5C10.8062 1.5 9.66157 1.97411 8.81766 2.81802C7.97374 3.66193 7.49964 4.80653 7.49964 6H4.34308C3.97399 5.99744 3.61691 6.13106 3.34016 6.37529C3.06342 6.61952 2.88644 6.95721 2.84308 7.32375L1.5062 18.5737C1.4817 18.7837 1.50186 18.9965 1.56536 19.1981C1.62885 19.3998 1.73425 19.5857 1.87464 19.7438C2.01604 19.9025 2.18932 20.0296 2.38317 20.1168C2.57701 20.204 2.78707 20.2494 2.99964 20.25H20.9921C21.206 20.2505 21.4175 20.2056 21.6127 20.1183C21.8079 20.0311 21.9824 19.9034 22.1246 19.7438C22.2644 19.5854 22.3691 19.3993 22.4319 19.1977C22.4948 18.9961 22.5143 18.7835 22.4893 18.5737ZM11.9996 3C12.7953 3 13.5584 3.31607 14.121 3.87868C14.6836 4.44129 14.9996 5.20435 14.9996 6H8.99964C8.99964 5.20435 9.31571 4.44129 9.87832 3.87868C10.4409 3.31607 11.204 3 11.9996 3ZM2.99964 18.75L4.34308 7.5H7.49964V9.75C7.49964 9.94891 7.57866 10.1397 7.71931 10.2803C7.85996 10.421 8.05073 10.5 8.24964 10.5C8.44855 10.5 8.63932 10.421 8.77997 10.2803C8.92062 10.1397 8.99964 9.94891 8.99964 9.75V7.5H14.9996V9.75C14.9996 9.94891 15.0787 10.1397 15.2193 10.2803C15.36 10.421 15.5507 10.5 15.7496 10.5C15.9486 10.5 16.1393 10.421 16.28 10.2803C16.4206 10.1397 16.4996 9.94891 16.4996 9.75V7.5H19.6637L20.9921 18.75H2.99964Z" fill="currentColor"/>
          </svg>
          ADD TO CART
        </button>
      </div>
    </div>
  </dialog>
</div>

{% stylesheet %}
  /* ===== Arenus Product Recommendations — Mobile-first ===== */
  /* Identical styling to arenus-products carousel */

  .arenus-product-recs {
    background-color: var(--arenus-product-recs-bg, #ffffff);
    color: var(--arenus-product-recs-text, #000000);
    display: flex;
    flex-direction: column;
    gap: 20px;
    margin: 0 auto;
    max-width: 1440px;
  }

  /* Header row */
  .arenus-product-recs__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-inline: 20px;
  }

  /* Section heading */
  .arenus-product-recs__heading {
    font-family: var(--font-heading--family);
    font-style: var(--font-heading--style);
    font-weight: var(--font-heading--weight);
    font-size: 32px;
    line-height: 1.1;
    letter-spacing: -0.64px;
    margin: 0;
  }

  /* Navigation buttons — hidden on mobile */
  .arenus-product-recs__nav {
    display: none;
  }

  /* Carousel container */
  .arenus-product-recs__carousel {
    overflow: hidden;
  }

  /* Scrollable track */
  .arenus-product-recs__track {
    display: flex;
    gap: 16px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;

    /* Hide scrollbar but keep functionality */
    scrollbar-width: none;
    -ms-overflow-style: none;
  }

  .arenus-product-recs__track::-webkit-scrollbar {
    display: none;
  }

  /* Product card */
  .arenus-product-recs__card {
    position: relative;
    flex: 0 0 320px;
    max-width: 320px;
    scroll-snap-align: center;
    display: flex;
    flex-direction: column;
    gap: 20px;
    text-decoration: none;
    color: inherit;
    cursor: pointer;
  }

  /* Invisible overlay link — makes entire card clickable */
  .arenus-product-recs__card-link {
    position: absolute;
    inset: 0;
    z-index: 1;
    text-decoration: none;
  }

  .arenus-product-recs__card:first-child {
    margin-left: 20px;
  }

  .arenus-product-recs__card:last-child {
    margin-right: 20px;
  }

  /* Image area */
  .arenus-product-recs__card-image {
    aspect-ratio: 1;
    background-color: var(--arenus-product-recs-image-bg, #fffcea);
    border-radius: 12px;
    padding: 16px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .arenus-product-recs__card-image img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    aspect-ratio: 1/1;
  }

  /* Info area */
  .arenus-product-recs__card-info {
    display: flex;
    flex-direction: column;
    gap: 12px;
    min-width: 0;
  }

  /* Star rating row */
  .arenus-product-recs__rating {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .arenus-product-recs__stars {
    display: flex;
    align-items: center;
    gap: 0;
  }

  .arenus-product-recs__stars svg {
    width: 16px;
    height: 16px;
    flex-shrink: 0;
  }

  .arenus-product-recs__review-count {
    font-family: var(--font-body--family);
    font-weight: var(--font-body--weight);
    font-size: 14px;
    line-height: 1;
    color: #808085;
  }

  /* Title + price row */
  .arenus-product-recs__title-price {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    min-width: 0;
    overflow: hidden;
  }

  .arenus-product-recs__title {
    font-family: var(--font-body--family);
    font-weight: var(--font-body--weight);
    font-size: 20px;
    line-height: 1.4;
    letter-spacing: -0.4px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    flex: 1;
    min-width: 0;
  }

  .arenus-product-recs__price {
    font-family: var(--font-body--family);
    font-weight: 700;
    font-size: 16px;
    line-height: 1.6;
    letter-spacing: -0.32px;
    white-space: nowrap;
  }

  /* Tag badges */
  .arenus-product-recs__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .arenus-product-recs__tag {
    background-color: #f9f9f9;
    border-radius: 4px;
    padding: 4px 8px;
    font-family: var(--font-body--family);
    font-weight: var(--font-body--weight);
    font-size: 14px;
    line-height: 1;
  }

  /* ===== Card Add-to-Cart Button ===== */
  /* Mobile: button is positioned at the very bottom of the card */
  .arenus-product-recs__add-btn {
    position: absolute;
    z-index: 2;
    bottom: 0;
    left: 0;
    right: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 4px;
    width: 100%;
    padding: 12px 16px;
    background-color: #e6b236;
    color: #ffffff;
    border: none;
    border-radius: 6px;
    font-family: var(--font-body--family);
    font-weight: 500;
    font-size: 14px;
    line-height: 1;
    text-transform: uppercase;
    letter-spacing: 0;
    cursor: pointer;
  }

  .arenus-product-recs__add-btn.is-sold-out {
    background-color: #d4d4d4;
    cursor: not-allowed;
  }

  .arenus-product-recs__add-btn.is-loading {
    opacity: 0.6;
    pointer-events: none;
  }

  .arenus-product-recs__add-btn svg {
    width: 20px;
    height: 20px;
    flex-shrink: 0;
  }

  /* Reserve space at the bottom of the card for the button */
  .arenus-product-recs__card-info {
    padding-bottom: 56px;
  }

  /* ===== Desktop ===== */
  @media screen and (min-width: 750px) {
    .arenus-product-recs {
      gap: 32px;
      padding-inline: 40px;
    }

    .arenus-product-recs__header {
      padding-inline: 0;
    }

    .arenus-product-recs__nav {
      display: flex;
      align-items: center;
      gap: 0;
    }

    .arenus-product-recs__nav-btn {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 8px;
      border: none;
      border-radius: 100px;
      background: transparent;
      cursor: pointer;
      color: inherit;
      transition: opacity 0.2s ease;
    }

    .arenus-product-recs__nav-btn:hover {
      opacity: 0.6;
    }

    .arenus-product-recs__nav-btn svg {
      width: 24px;
      height: 24px;
    }

    .arenus-product-recs__track {
      gap: 20px;
    }

    .arenus-product-recs__card {
      flex: 0 0 325px;
      max-width: 325px;
    }

    .arenus-product-recs__card:first-child {
      margin-left: 0;
    }

    .arenus-product-recs__card:last-child {
      margin-right: 0;
    }

    /* Desktop: image is containing block for the button */
    .arenus-product-recs__card-image {
      position: relative;
      overflow: visible;
    }

    /* Desktop: button overlays the image, hidden by default, fade in on hover */
    .arenus-product-recs__add-btn {
      bottom: 26px;
      left: 50%;
      right: auto;
      transform: translateX(-50%);
      width: 100%;
      max-width: 225px;
      padding: 16px 32px;
      font-size: 16px;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.15s ease;
    }

    .arenus-product-recs__card:hover .arenus-product-recs__add-btn {
      opacity: 1;
      pointer-events: auto;
    }

    .arenus-product-recs__add-btn:hover {
      opacity: 0.85;
    }

    /* Desktop: remove bottom padding reservation */
    .arenus-product-recs__card-info {
      padding-bottom: 0;
    }
  }
{% endstylesheet %}

<script>
  /* ===== Arenus Product Recommendations — Carousel Nav + Quick Add ===== */
  (function() {
    var section = document.getElementById('ArenusProductRecs-{{ section.id }}');
    if (!section) return;

    /* --- Fetch recommendations via Section Rendering API --- */
    function loadRecommendations() {
      var url = section.getAttribute('data-url');
      if (!url) return;

      fetch(url)
        .then(function(res) { return res.text(); })
        .then(function(html) {
          var parser = new DOMParser();
          var doc = parser.parseFromString(html, 'text/html');
          var newSection = doc.getElementById('ArenusProductRecs-{{ section.id }}');
          if (!newSection) return;

          /* Replace carousel + nav visibility */
          var newCarousel = newSection.querySelector('.arenus-product-recs__carousel');
          var newNav = newSection.querySelector('.arenus-product-recs__nav');
          var currentCarouselParent = section;

          /* Remove old carousel if present */
          var oldCarousel = section.querySelector('.arenus-product-recs__carousel');
          if (oldCarousel) oldCarousel.remove();

          /* Insert new carousel before the dialog */
          var dialogEl = section.querySelector('.arenus-qam__dialog');
          if (newCarousel) {
            if (dialogEl) {
              section.insertBefore(newCarousel, dialogEl);
            } else {
              section.appendChild(newCarousel);
            }
          }

          /* Show nav buttons if products > 1 */
          var navEl = section.querySelector('.arenus-product-recs__nav');
          var cards = section.querySelectorAll('.arenus-product-recs__card');
          if (navEl && cards.length > 1) {
            navEl.style.display = '';
          }

          /* Initialize carousel + quick add after DOM is updated */
          initCarousel();
          initQuickAdd();
        })
        .catch(function(err) {
          console.error('Failed to load product recommendations:', err);
        });
    }

    /* --- Carousel navigation --- */
    function initCarousel() {
      var track = section.querySelector('.arenus-product-recs__track');
      var prevBtn = section.querySelector('.arenus-product-recs__nav-btn--prev');
      var nextBtn = section.querySelector('.arenus-product-recs__nav-btn--next');

      if (track && prevBtn && nextBtn) {
        function getCardWidth() {
          var card = track.querySelector('.arenus-product-recs__card');
          if (!card) return 0;
          var style = window.getComputedStyle(track);
          var gap = parseFloat(style.columnGap) || 0;
          return card.offsetWidth + gap;
        }

        prevBtn.addEventListener('click', function() {
          track.scrollBy({ left: -getCardWidth(), behavior: 'smooth' });
        });

        nextBtn.addEventListener('click', function() {
          track.scrollBy({ left: getCardWidth(), behavior: 'smooth' });
        });
      }
    }

    /* --- Quick Add Modal --- */
    function initQuickAdd() {
      var dialog = section.querySelector('.arenus-qam__dialog');
      if (!dialog) return;

      var currentProductData = null;
      var currentSubInstance = null;
      var selectedOptions = [];
      var quantity = 1;
      var isAdding = false;

      var modalTitle = dialog.querySelector('.arenus-qam__title');
      var modalPrice = dialog.querySelector('.arenus-qam__price');
      var modalComparePrice = dialog.querySelector('.arenus-qam__compare-price');
      var modalImage = dialog.querySelector('.arenus-qam__image img');
      var modalOptions = dialog.querySelector('.arenus-qam__options');
      var modalAddBtn = dialog.querySelector('.arenus-qam__add-btn');
      var modalCloseBtn = dialog.querySelector('.arenus-qam__close');
      var stepperValue = dialog.querySelector('.arenus-qam__stepper-value');
      var stepperMinus = dialog.querySelector('.arenus-qam__stepper-minus');
      var stepperPlus = dialog.querySelector('.arenus-qam__stepper-plus');

      /* --- Format price (cents to dollars) --- */
      function formatPrice(cents) {
        return '$' + (cents / 100).toFixed(2);
      }

      /* --- Find matching variant from selected options --- */
      function findVariant() {
        if (!currentProductData) return null;
        for (var i = 0; i < currentProductData.variants.length; i++) {
          var v = currentProductData.variants[i];
          var match = true;
          for (var j = 0; j < selectedOptions.length; j++) {
            if (v.options[j] !== selectedOptions[j]) {
              match = false;
              break;
            }
          }
          if (match) return v;
        }
        return null;
      }

      /* --- Check if a specific option value leads to any available variant --- */
      function isOptionAvailable(optionIndex, optionValue) {
        if (!currentProductData) return false;
        for (var i = 0; i < currentProductData.variants.length; i++) {
          var v = currentProductData.variants[i];
          if (v.options[optionIndex] !== optionValue) continue;
          /* Check that all OTHER selected options also match */
          var compatible = true;
          for (var j = 0; j < selectedOptions.length; j++) {
            if (j === optionIndex) continue;
            if (v.options[j] !== selectedOptions[j]) {
              compatible = false;
              break;
            }
          }
          if (compatible && v.available) return true;
        }
        return false;
      }

      /* --- Build option group HTML --- */
      function buildOptions() {
        if (!currentProductData || !modalOptions) return;
        modalOptions.innerHTML = '';

        var options = currentProductData.options;
        for (var i = 0; i < options.length; i++) {
          var opt = options[i];
          var group = document.createElement('div');
          group.className = 'arenus-qam__option-group';

          var label = document.createElement('p');
          label.className = 'arenus-qam__option-label';
          label.textContent = opt.name + ':';
          group.appendChild(label);

          var valuesWrap = document.createElement('div');
          valuesWrap.className = 'arenus-qam__option-values';

          for (var j = 0; j < opt.values.length; j++) {
            var val = opt.values[j];
            var btn = document.createElement('button');
            btn.type = 'button';
            btn.className = 'arenus-qam__option-btn';
            btn.textContent = val;
            btn.setAttribute('data-option-index', i);
            btn.setAttribute('data-option-value', val);

            if (selectedOptions[i] === val) {
              btn.classList.add('is-selected');
            }

            if (!isOptionAvailable(i, val)) {
              btn.classList.add('is-unavailable');
            }

            valuesWrap.appendChild(btn);
          }

          group.appendChild(valuesWrap);
          modalOptions.appendChild(group);
        }
      }

      /* --- Update modal prices --- */
      function updateModalPrices(variant) {
        if (modalPrice) {
          modalPrice.textContent = variant ? formatPrice(variant.price) : '';
        }
        if (modalComparePrice) {
          var hasCompare = variant && variant.compare_at_price && variant.compare_at_price > variant.price;
          modalComparePrice.textContent = hasCompare ? formatPrice(variant.compare_at_price) : '';
        }
      }

      /* --- Update modal state after option change --- */
      function updateModalState() {
        var variant = findVariant();

        /* Rebuild option pills to update unavailable states */
        buildOptions();
        updateModalPrices(variant);

        if (variant) {
          modalAddBtn.disabled = !variant.available;
        } else {
          modalAddBtn.disabled = true;
        }

        if (currentSubInstance && variant) {
          currentSubInstance.setVariant(variant.id);
        }
      }

      /* --- Option pill click handler (delegated) --- */
      if (modalOptions) {
        modalOptions.addEventListener('click', function(e) {
          var btn = e.target.closest('.arenus-qam__option-btn');
          if (!btn) return;

          var optionIndex = parseInt(btn.getAttribute('data-option-index'), 10);
          var optionValue = btn.getAttribute('data-option-value');

          selectedOptions[optionIndex] = optionValue;
          updateModalState();
        });
      }

      /* --- Quantity stepper --- */
      if (stepperMinus) {
        stepperMinus.addEventListener('click', function() {
          if (quantity > 1) {
            quantity--;
            stepperValue.textContent = quantity;
          }
        });
      }

      if (stepperPlus) {
        stepperPlus.addEventListener('click', function() {
          quantity++;
          stepperValue.textContent = quantity;
        });
      }

      /* --- Open modal for a product --- */
      function openModal(productData) {
        currentProductData = productData;
        quantity = 1;

        /* Set default selected options (first value of each option) */
        selectedOptions = [];
        for (var i = 0; i < productData.options.length; i++) {
          selectedOptions.push(productData.options[i].values[0]);
        }

        /* Populate modal */
        if (modalTitle) modalTitle.textContent = productData.title;
        if (modalImage) {
          modalImage.src = productData.image;
          modalImage.alt = productData.title;
        }
        if (stepperValue) stepperValue.textContent = '1';

        /* Update Stamped badge in modal */
        var modalBadge = dialog.querySelector('.arenus-qam__stamped-badge');
        if (modalBadge) {
          modalBadge.setAttribute('data-id', productData.id);
          modalBadge.setAttribute('data-product-sku', productData.handle || '');
          modalBadge.setAttribute('data-product-title', productData.title || '');
          modalBadge.innerHTML = '';
          if (typeof StampedFn !== 'undefined' && StampedFn.loadBadges) { StampedFn.loadBadges(); }
        }

        /* Build option pills */
        buildOptions();

        /* Enable add button if default variant is valid */
        var variant = findVariant();
        updateModalPrices(variant);
        if (modalAddBtn) {
          modalAddBtn.disabled = !variant || !variant.available;
        }

        /* Render subscription selector */
        try {
          var subWrap = dialog.querySelector('[data-sub-wrap]');
          if (subWrap && window.ArenusSubscription) {
            currentSubInstance = window.ArenusSubscription.render(subWrap, productData, {
              variantId: variant ? variant.id : productData.variants[0].id
            });
          }
        } catch (err) {
          console.error('Subscription render failed:', err);
        }

        /* Show dialog */
        dialog.classList.remove('is-closing');
        dialog.showModal();

        /* Lock body scroll */
        var scrollY = window.scrollY;
        dialog._previousScrollY = scrollY;
        document.body.style.width = '100%';
        document.body.style.position = 'fixed';
        document.body.style.top = '-' + scrollY + 'px';
      }

      /* --- Close modal --- */
      function closeModal() {
        dialog.classList.add('is-closing');

        function onEnd() {
          dialog.removeEventListener('animationend', onEnd);
          dialog.close();
          dialog.classList.remove('is-closing');

          /* Restore body scroll */
          document.body.style.width = '';
          document.body.style.position = '';
          document.body.style.top = '';
          if (dialog._previousScrollY !== undefined) {
            window.scrollTo({ top: dialog._previousScrollY, behavior: 'instant' });
          }

          if (currentSubInstance) { currentSubInstance.destroy(); currentSubInstance = null; }
          currentProductData = null;
          selectedOptions = [];
          quantity = 1;
        }

        dialog.addEventListener('animationend', onEnd);
      }

      /* Close button */
      if (modalCloseBtn) {
        modalCloseBtn.addEventListener('click', function() {
          closeModal();
        });
      }

      /* Backdrop click (click on dialog itself, not its children) */
      dialog.addEventListener('click', function(e) {
        if (e.target === dialog) {
          closeModal();
        }
      });

      /* Escape key */
      dialog.addEventListener('keydown', function(e) {
        if (e.key === 'Escape') {
          e.preventDefault();
          closeModal();
        }
      });

      /* Prevent native dialog cancel (Escape fires cancel before close) */
      dialog.addEventListener('cancel', function(e) {
        e.preventDefault();
        closeModal();
      });

      /* --- Button loading state --- */
      function setButtonLoading(btn, loading) {
        if (!btn) return;
        if (loading) {
          btn._prevHTML = btn.innerHTML;
          btn.classList.add('is-loading');
          btn.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true"><path d="M22.4893 18.5737L21.1525 7.32375C21.1091 6.95721 20.9321 6.61952 20.6554 6.37529C20.3786 6.13106 20.0215 5.99744 19.6525 6H16.4996C16.4996 4.80653 16.0255 3.66193 15.1816 2.81802C14.3377 1.97411 13.1931 1.5 11.9996 1.5C10.8062 1.5 9.66157 1.97411 8.81766 2.81802C7.97374 3.66193 7.49964 4.80653 7.49964 6H4.34308C3.97399 5.99744 3.61691 6.13106 3.34016 6.37529C3.06342 6.61952 2.88644 6.95721 2.84308 7.32375L1.5062 18.5737C1.4817 18.7837 1.50186 18.9965 1.56536 19.1981C1.62885 19.3998 1.73425 19.5857 1.87464 19.7438C2.01604 19.9025 2.18932 20.0296 2.38317 20.1168C2.57701 20.204 2.78707 20.2494 2.99964 20.25H20.9921C21.206 20.2505 21.4175 20.2056 21.6127 20.1183C21.8079 20.0311 21.9824 19.9034 22.1246 19.7438C22.2644 19.5854 22.3691 19.3993 22.4319 19.1977C22.4948 18.9961 22.5143 18.7835 22.4893 18.5737ZM11.9996 3C12.7953 3 13.5584 3.31607 14.121 3.87868C14.6836 4.44129 14.9996 5.20435 14.9996 6H8.99964C8.99964 5.20435 9.31571 4.44129 9.87832 3.87868C10.4409 3.31607 11.204 3 11.9996 3ZM2.99964 18.75L4.34308 7.5H7.49964V9.75C7.49964 9.94891 7.57866 10.1397 7.71931 10.2803C7.85996 10.421 8.05073 10.5 8.24964 10.5C8.44855 10.5 8.63932 10.421 8.77997 10.2803C8.92062 10.1397 8.99964 9.94891 8.99964 9.75V7.5H14.9996V9.75C14.9996 9.94891 15.0787 10.1397 15.2193 10.2803C15.36 10.421 15.5507 10.5 15.7496 10.5C15.9486 10.5 16.1393 10.421 16.28 10.2803C16.4206 10.1397 16.4996 9.94891 16.4996 9.75V7.5H19.6637L20.9921 18.75H2.99964Z" fill="currentColor"/></svg> ADDING...';
        } else {
          btn.classList.remove('is-loading');
          if (btn._prevHTML !== undefined) btn.innerHTML = btn._prevHTML;
        }
      }

      /* --- Add to cart (shared for both direct + modal) --- */
      function addToCart(variantId, qty, triggerBtn, onDone, sellingPlanId) {
        if (isAdding) return;
        isAdding = true;
        setButtonLoading(triggerBtn, true);

        var body = { id: variantId, quantity: qty };
        if (sellingPlanId) body.selling_plan = parseInt(sellingPlanId, 10);

        fetch('/cart/add.js', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Accept': 'application/json'
          },
          body: JSON.stringify(body)
        })
        .then(function(res) { return res.json(); })
        .then(function(data) {
          if (data.status) {
            /* Error from Shopify (e.g., out of stock) */
            console.error('Add to cart error:', data.description);
            isAdding = false;
            setButtonLoading(triggerBtn, false);
            return;
          }

          /* Dispatch cart:update event so cart drawer auto-opens + icon updates */
          document.dispatchEvent(new CustomEvent('cart:update', {
            bubbles: true,
            detail: {
              resource: {},
              sourceId: '',
              data: {
                source: 'arenus-quick-add',
                itemCount: qty
              }
            }
          }));

          /* Also explicitly open the cart drawer in case auto-open is disabled */
          var cartDrawer = document.querySelector('cart-drawer-component');
          if (cartDrawer) {
            if (typeof cartDrawer.open === 'function') {
              cartDrawer.open();
            } else if (typeof cartDrawer.showDialog === 'function') {
              cartDrawer.showDialog();
            }
          }

          isAdding = false;
          setButtonLoading(triggerBtn, false);
          if (typeof onDone === 'function') onDone();
        })
        .catch(function(err) {
          console.error('Add to cart fetch error:', err);
          isAdding = false;
          setButtonLoading(triggerBtn, false);
        });
      }

      /* --- Modal add-to-cart button --- */
      if (modalAddBtn) {
        modalAddBtn.addEventListener('click', function() {
          var variant = findVariant();
          if (!variant || !variant.available) return;

          var sellingPlanId = currentSubInstance ? currentSubInstance.getSellingPlanId() : null;
          addToCart(variant.id, quantity, modalAddBtn, function() {
            closeModal();
          }, sellingPlanId);
        });
      }

      /* --- Card button click handlers --- */
      var addBtns = section.querySelectorAll('.arenus-product-recs__add-btn');

      addBtns.forEach(function(btn) {
        btn.addEventListener('click', function(e) {
          e.preventDefault();
          e.stopPropagation();

          var isSingleVariant = btn.hasAttribute('data-single-variant');

          if (isSingleVariant) {
            /* Direct add to cart — no modal */
            var variantId = parseInt(btn.getAttribute('data-variant-id'), 10);
            addToCart(variantId, 1, btn);
          } else {
            /* Open modal with product data */
            var card = btn.closest('.arenus-product-recs__card');
            if (!card) return;

            var scriptTag = card.querySelector('.arenus-product-recs__card-data');
            if (!scriptTag) return;

            try {
              var productData = JSON.parse(scriptTag.textContent);
              openModal(productData);
            } catch (err) {
              console.error('Failed to parse product data:', err);
            }
          }
        });
      });
    }

    /* --- On page load: either init from server-rendered HTML or fetch via AJAX --- */
    var existingCarousel = section.querySelector('.arenus-product-recs__carousel');
    if (existingCarousel) {
      /* Products were already rendered server-side (section rendering API response) */
      var navEl = section.querySelector('.arenus-product-recs__nav');
      var cards = section.querySelectorAll('.arenus-product-recs__card');
      if (navEl && cards.length > 1) {
        navEl.style.display = '';
      }
      initCarousel();
      initQuickAdd();
    } else {
      /* Initial page load — fetch recommendations via AJAX */
      loadRecommendations();
    }
  })();
</script>

{% schema %}
{
  "name": "Product Recommendations",
  "tag": "section",
  "class": "arenus-product-recs-wrapper section-wrapper",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "You May Also Like"
    },
    {
      "type": "range",
      "id": "max_products",
      "label": "Maximum products",
      "min": 2,
      "max": 16,
      "step": 1,
      "default": 8
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#000000"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 72
    }
  ],
  "templates": ["product"]
}
{% endschema %}
